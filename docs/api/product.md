# 상품 API 구현 기록

## 구현 범위

- 상품 초안 등록, 부분 수정, 논리 삭제
  - 수정은 `DRAFT`, `ON_SALE`, `HIDDEN`에서 허용한다. 등록을 끝낸 뒤 발견하는 가격 오타 같은
    실수를 판매자가 스스로 고칠 수 있어야 하기 때문이다. `RESERVED` 이후는 구매자가 그 조건을
    보고 결제·검수에 들어간 뒤라 `PRODUCT_EDIT_NOT_ALLOWED`로 거절한다.
- 필수 체크리스트 완료 검증을 포함한 `DRAFT -> ON_SALE` 전환
- 판매자가 직접 종료하는 `ON_SALE|HIDDEN -> SOLD` 전환
  - 서비스 결제를 거치지 않는 직거래를 정리하기 위한 출구다. 결제 흐름
    (`RESERVED -> PAID -> INSPECTING -> CONFIRMED -> SETTLED`)과는 별개 경로이며,
    `RESERVED` 이후에는 구매자가 이미 결제·검수에 들어가 있어 거절한다.
  - `listing.status`는 `VARCHAR(30)`이라 `SOLD` 추가에 마이그레이션이 필요하지 않다.
  - 공개 목록·상세는 `ON_SALE`만 노출하므로 `SOLD` 매물은 구매자에게 `LISTING_NOT_FOUND`로
    응답한다. 판매자는 `GET /api/v1/members/me/products/{productId}`로 계속 확인할 수 있다.
- `ON_SALE` 공개 상품 목록·상세 및 판매자 본인 상품 목록·상세 조회
- 기기 카테고리·모델·체크리스트 템플릿·판매 준비 가이드 조회
- 상품 생성 시 게시된 체크리스트 템플릿을 매물 항목으로 스냅샷 저장
- 관심상품 등록·목록·해제와 판매자 상품 관리 화면 API 연동

상품 등록, 수정, 삭제, 상태 변경과 판매자 본인 상품 조회는 JWT의 `SELLER` 역할이
필수다. 서버는 토큰 역할 검사 후에도 `seller` 테이블의 현재 상태가 `ACTIVE`인지 확인한다.
따라서 일반 회원은 상품을 등록할 수 없고, 판매자 정지 직전에 발급된 토큰도 상품 변경에
사용할 수 없다.

증거 업로드·확인·재촬영 이력, OCR, batteryreport/dxdiag 파싱, 자동 추출값 확정,
채팅 및 영상통화는 담당 범위에서 제외했으며 기존 목업을 변경하지 않았다.

### 카탈로그에 없는 기기 모델 요청

기기 모델은 `category` 테이블의 실제 행이고 `checklist_template`이 그 행에 묶여 있어 임의
문자열을 `deviceModelId`로 보낼 수 없다. 시드 이전에는 모델이 7종뿐이라 그 목록에 없는 기기를
가진 판매자는 상품을 등록할 방법이 아예 없었다.

판매 화면은 모델을 제조사별로 묶어 보여주며, `model_code`가 `ETC-`로 시작하는 내부 carrier
행은 노출하지 않는다. 찾는 모델이 없으면 판매자가 제조사·모델명·모델 코드·운영체제를 입력해
`POST /api/v1/device-model-requests`로 검토를 요청한다.

- 요청은 `PENDING` 상태로 저장되고, 이 단계에서는 상품이나 판매 체크리스트를 만들지 않는다.
- 관리자가 승인하면 실제 카탈로그 모델과 기본 `PUBLISHED` 템플릿을 만든다.
- 판매자는 승인된 모델을 목록에서 선택한 뒤 상품을 등록한다.
- 처음 선택된 모델의 AI 조사는 모델별로 한 번만 수행되며, 관리자 승인 전에는 기본 템플릿만
  상품 체크리스트에 사용한다.
- 기존 직접 입력 클라이언트와 이미 저장된 매물의 호환을 위해 carrier 및
  `listing.custom_manufacturer`, `listing.custom_model_name` 계약은 유지한다. 신규 판매 화면은
  이 호환 경로를 사용하지 않는다.

## 데이터 변경

`V20260727__extend_product_fields.sql`에서 다음 필드를 추가한다.

- `listing.color`, `listing.storage_gb`, `listing.trade_region`
- `category.manufacturer_id`, `category.supported_storage_gb`
- `category.manufacturer_id` 조회 인덱스
- 공개 목록, 판매자 목록, 관심상품 최신순, 썸네일 일괄 조회용 복합 인덱스

공개 상세 `GET /api/v1/products/{productId}`는 `ON_SALE` 상품만 반환한다. 활성 판매자는
`SELLER` 역할이 필요한 `GET /api/v1/members/me/products/{productId}`로 본인의 `DRAFT`,
`HIDDEN` 상품을 조회한다.
목록의 `manufacturerId`, `verificationStatus(COMPLETED|IN_PROGRESS)`, `sort` 조건은 DB 조회에
반영하며 허용되지 않은 정렬 필드나 방향은 `INVALID_INPUT_VALUE`로 거절한다.

목록 응답 조립 시 상품별 조회를 반복하지 않는다. 카테고리는 entity graph로 함께 읽고,
체크리스트 집계와 썸네일은 현재 페이지의 상품 ID를 묶어 각각 한 번씩 조회한다.
애플리케이션 시간대는 `APPLICATION_TIME_ZONE`으로 관리하며 기본값은 `Asia/Seoul`이다.
JVM 기본 시간대와 상품 `OffsetDateTime` 변환이 같은 기준을 사용하므로 실행 호스트가 UTC여도
저장·응답 시각은 `+09:00` 계약을 유지한다.

## 수치 기록

| 항목 | 변경 전 | 변경 후 | 변화 |
|---|---:|---:|---:|
| 백엔드 단위·컨트롤러 테스트 | 107 | 111 | +4 |
| 프론트엔드 테스트 | 28 | 30 | +2 |
| 상품/관심상품 DB 통합 테스트 | 5 | 7 | +2 |
| 상품·카탈로그 컨트롤러의 고정 응답 메서드 | 12개 | 0개 | -12개 |

리뷰 반영 후 추가 개선은 다음과 같다.

| 항목 | 리뷰 반영 전 | 리뷰 반영 후 | 변화 |
|---|---:|---:|---:|
| 백엔드 전체 단위·컨트롤러 테스트 | 111 | 122 | +11 |
| 프론트엔드 테스트 | 30 | 34 | +4 |
| DB 통합 테스트 | 7 | 8 | +1 |
| 공개 목록 100건의 예상 조회 쿼리 | 최대 약 402 | 4 | 약 99% 감소 |
| 상품 도메인 로그의 회원 식별자 필드 | 7곳 | 0곳 | -7곳 |

검증 결과: 백엔드 테스트 122건, 프론트엔드 테스트 34건, DB 통합 테스트 8건이
모두 통과했다. 목록 쿼리 수는 page/count 2회, 체크리스트 일괄 집계 1회, 썸네일 일괄 조회
1회를 기준으로 계산했다.

## 트러블슈팅 기록

1. JPA 자동 설정을 제외한 컨텍스트 테스트에서 신규 상품 서비스가 `CategoryRepository`를 찾지 못해
   18건이 연쇄 실패했다. 해당 테스트의 관심 대상이 아닌 상품 서비스를 `@MockitoBean`으로 격리해
   전체 테스트를 복구했다.
2. 관심상품 통합 테스트 SQL에 Java 숫자 구분 표기(`98_001`)가 들어가 MySQL이 컬럼명으로 해석했다.
   SQL 숫자 리터럴을 `98001`, `99001`로 수정해 7건 통합 테스트를 통과시켰다.
3. PowerShell 실행 정책이 `npm.ps1`을 차단했다. 동일 npm 실행 파일인 `npm.cmd`로 검증 명령을
   실행해 lint, test, build를 완료했다.
4. 샌드박스에서 Gradle 배포 파일과 Vite 설정 접근이 제한됐다. 승인된 검증 명령만 외부 실행해
   의존성 다운로드와 빌드 검증을 완료했다.
5. MR 최초 CI의 `secret_guard`가 EC2 DNS 장애로 GitLab 소스를 받지 못해 실패했다. 네트워크
   회복 후 작업과 파이프라인을 재시도해 전체 CI를 통과시켰다.
6. 관심상품 동시 등록 테스트를 위해 클래스 수준 테스트 트랜잭션을 제거하자 기존 삭제 테스트가
   트랜잭션 없이 수정 쿼리를 호출했다. 삭제 테스트에만 트랜잭션을 명시하고 동시성 테스트는 서로
   독립된 두 트랜잭션으로 유지해 MySQL에서 8건을 통과시켰다.
