# Claude 프로젝트 지침

공통 규칙은 루트 `AGENTS.md`를 따른다. 작업 대상이 `frontend/` 또는 `backend/`이면 해당 폴더의 `AGENTS.md`도 읽는다.

`.claude/settings.json`의 Hook이 작업을 차단하면 우회하지 말고 차단 이유를 보고한다.

## 컨텍스트·토큰 절약

- 여러 파일 탐색이나 반복적인 grep·read가 필요한 조사는 가능하면 Task(서브에이전트)로 위임하고, 원문 전체가 아니라 결과 요약만 받아 메인 대화 컨텍스트에 남긴다.
- 이미 읽은 파일은 다시 통째로 Read하지 않는다. 변경 여부 확인이 필요하면 diff나 필요한 범위만 다시 본다.
- 큰 파일은 처음부터 전체를 읽지 않는다. Grep으로 위치를 먼저 찾은 뒤 Read의 offset·limit으로 필요한 범위만 읽는다.
- 진행 상황을 단계마다 길게 중계하지 않고, 완료 시점에 한 번 요약해서 보고한다.

