package com.c203.limit.domain.inspection.enums;

/**
 * 계정 제거 가이드가 적용되는 기기 대분류.
 * account_removal_guide.device_type 매핑.
 *
 * 주의: 상품 도메인의 category.device_type과 값 집합이 동일하다.
 * 팀에서 공용 모듈(shared-kernel)을 두기로 하면 그쪽 enum을 참조하도록
 * 바꾸고 이 파일은 제거하는 게 낫다 — 지금은 도메인 간 결합을 피하려고
 * 별도로 복제해 두었다.
 */
public enum DeviceType {
}
